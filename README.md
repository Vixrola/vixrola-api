VdZip backend - all-platform Decodo proxy version. All enabled platforms use the configured Decodo proxy for yt-dlp extraction and downloads. Pinterest is no longer excluded from the proxy. Required Render environment variables: DECODO_HOST DECODO_PORT DECODO_USERNAME DECODO_PASSWORD If the Decodo proxy itself returns a 522 tunnel error, the affected request will fail until the proxy endpoint/credentials are fixed.
